apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "laravel.fullname" . }}
spec:
  to:
    kind: Service
    name: {{ include "laravel.fullname" . }}
  port:
    targetPort: 8080
  tls:
    termination: edge
